language: python
sudo: true
python:
  - "3.7"
  - "3.8"
install:
  - pip install pipenv
  - pipenv install
script:
  - pytest tests/ --cov-report term-missing --cov=flapjack --cov-report=xml
after_success:
  - bash <(curl -s https://codecov.io/bash)
