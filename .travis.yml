language: python
sudo: true
python:
  - "3.7"
  - "3.8"
install:
  - pip install pipenv
  - pipenv install
script:
  - pytest --cov-report term --cov=flapjack --cov-report=xml
after_success:
  - bash <(curl -s https://codecov.io/bash)
